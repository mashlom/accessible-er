# CLAUDE.md

הנחיות עבודה על הריפו הזה. **קִראו את הקובץ הזה לפני שמשנים קוד.**

## מה זה

`accessible-er` — אפליקציית **דמו** להנגשת הביקור במלר״ד ילדים בבית החולים לניאדו
עבור ילדים אוטיסטים ומשפחותיהם.

- התוכן (שלבי הביקור, הפרוצדורות, המפה, כרטיס ההתאמות, ההנחיות להורים) הוא פרי
  עבודתם של **אלו״ט** ושל **צוות המלר״ד**. אל תמציאו תוכן רפואי ואל "תשפרו" ניסוחים
  קליניים על דעת עצמכם — שינוי תוכן דורש אישור של אנשי התוכן.
- **זו לא האפליקציה הסופית.** מטרתה להראות קונספט ועיצוב, ולאפשר לרופאים ולצוות
  להחליט איך תיראה האפליקציה האמיתית.
- האפיון המקורי והפידבק הראשון נמצאים ב־[spec/](spec/) (קבצי `.docx`).

## אילוצים שאסור לשבור

- **עברית, RTL מלא.** כל הטקסט בממשק בעברית. אין `dir="ltr"` חוץ מהצגת URL.
- **Mobile first**, אבל חייב להיראות טוב גם בטאבלט ובדסקטופ.
- **HashRouter בלבד** — האפליקציה היא SPA שמותקנת על GitHub Pages ואין שרת שיטפל
  ב־deep links. אין להחליף ל־`BrowserRouter`.
- **Client-side בלבד. אין backend, אין איסוף מידע מזהה, אין שליחה אוטומטית של
  שום דבר.** כל מידע המשתמש נשמר ב־`localStorage` עם תפוגה של 24 שעות (או
  ב־`sessionStorage` לאווטאר). מה שההורה לא מציג בעצמו — לא מגיע לצוות. אינטגרציה
  הורה↔צוות לא מומשה בכוונה, לפי האפיון.
- **נגישות:** יעדי מגע ≥48px, ניגודיות סבירה, `aria-*` על רכיבים אינטראקטיביים,
  כיבוד `prefers-reduced-motion` (מטופל גלובלית ב־[src/index.css](src/index.css)).
  קהל היעד רגיש חושית — אנימציות עדינות, בלי הבזקים ובלי צליל.

## ארכיטקטורה: קונספטים

הדף הראשי (`#/`) הוא **תפריט של קונספטים**. כל קונספט הוא כיוון UI/UX שונה על
**אותו תוכן בדיוק**, כדי שהצוות יוכל להשוות.

```
src/
  data/        תוכן משותף לכל הקונספטים — מקור אמת יחיד
  hooks/       usePersistentState, useVisitReason, useSessionAvatar
  lib/         storage.ts — localStorage עם תפוגה
  menu/        MenuPage — תפריט הקונספטים ב־"/"
  concepts/
    registry.ts        קטלוג הקונספטים שמוצג בתפריט
    nav.tsx            Link/Navigate מודעי־base (ראו למטה)
    calm/              קונספט 1 — "רגוע ומסודר"  → /calm
      CalmConcept.tsx  ה־<Routes> של הקונספט
      components/  pages/
    story/             קונספט 2 — "המסע של רוני" → /story
      StoryConcept.tsx
      components/  pages/  story.module.css  pages.module.css
  App.tsx      תפריט + מיפוי כל קונספט ל־base שלו
```

### כלל הזהב

**תוכן חי ב־`src/data`. קונספט הוא עור בלבד.**
אם אתם מוסיפים פרוצדורה, שלב במסע, אזור במפה או שדה בכרטיס — זה הולך ל־`src/data`,
וכל הקונספטים מקבלים את זה. אל תשכפלו טקסט לתוך קונספט.

### מצב משותף בין קונספטים

מפתחות ה־`localStorage` משותפים בכוונה, כך שמשפחה שמחליפה קונספט באמצע הביקור לא
מאבדת כלום:

| מפתח | מה | תפוגה |
| --- | --- | --- |
| `visit-reason` | סיבת ההגעה | 24ש׳ |
| `journey-stage` | "אנחנו כאן" במסע | 24ש׳ |
| `care-card` | כרטיס ההתאמות | 24ש׳ |
| `feedback` | המשוב שנשלח | 24ש׳ |
| `avatar` (sessionStorage) | הדמות של הילד/ה | סוף הסשן |

## איך מוסיפים קונספט חדש

1. `src/concepts/<id>/` עם `<Id>Concept.tsx` שמחזיר `<Routes>` פנימי
   (נתיבים **יחסיים**: `<Route path="map" .../>`, לא `/map`).
2. ב־[src/App.tsx](src/App.tsx):
   ```tsx
   <Route path="/<id>/*" element={<ConceptBase base="/<id>"><IdConcept /></ConceptBase>} />
   ```
3. הוסיפו רשומה ל־[src/concepts/registry.ts](src/concepts/registry.ts) — היא מה
   שמופיע בתפריט.
4. השתמשו רק בתוכן מ־`src/data`.

### `concepts/nav.tsx` — למה זה קיים

עמודי הקונספט כותבים קישורים אבסולוטיים לאפליקציה (`to="/journey"`), והעטיפות
ב־`nav.tsx` מוסיפות את ה־base של הקונספט הפעיל (`/calm/journey`). לכן:

- **בתוך קוד של קונספט** ייבאו `Link` ו־`Navigate` מ־`../../nav`, **לא** מ־
  `react-router-dom`. `useNavigate` (לחזרה אחורה) בא כרגיל מ־`react-router-dom`.
- `to="/"` בתוך קונספט פירושו "מסך הבית של הקונספט".
- כדי לחזור לתפריט הקונספטים משתמשים ב־`<a href="#/">` (מחוץ ל־base).

## שני הקונספטים הקיימים

### 1. `calm` — "רגוע ומסודר" (`#/calm`)

הקונספט המקורי. כרטיסי ניווט, צבעים רכים, טיפוגרפיה גדולה, עומס חושי נמוך.
משתמש בטוקנים הגלובליים (`--c-*`) שמוגדרים ב־`src/index.css`.

### 2. `story` — "המסע של רוני" (`#/story`)

עולם מצויר, "ספר סיפורים". אותו תוכן, חוויה אחרת:

- **רוני נוכח בכל מסך** — `RoniCompanion` הוא חלק מ־`StoryShell`, לא מהעמודים.
  הוא אומר משפט לפי המסך שבו נמצאים ([companionLines.ts](src/concepts/story/companionLines.ts)),
  ומגיב ללחיצה של הילד/ה (מצב־רוח + קפיצה). זה מכוון: הפידבק ביקש שהאווטאר יהיה
  משהו שהילד יכול "לעשות עליו מניפולציות" כדי לקבל תחושת שליטה.
  אפשר להקטין אותו לראש מציץ — ילד מוצף לא תמיד רוצה אנימציה.
- **הדמות של הילד/ה** (`useSessionAvatar`) מופיעה לצד רוני: במסך הבית, על התחנה
  הנוכחית בשביל, בתוך ציור הסיפור, ועל כרטיס ההתאמות.
- **מפת הדרכים** היא שביל אנכי עם תחנות (`TrailPage`), הפרוצדורות הן ספר עם
  דפדוף ומדליה בסוף (`BookPage`), המפה היא חדרים עם "מזג אוויר חושי" (`MapPage`).
- **טוקנים משלו** (`--s-*`) מוגדרים על `.theme` ב־
  [story.module.css](src/concepts/story/story.module.css). אל תשנו טוקנים גלובליים
  בשביל הקונספט הזה.
- `Roni` מקבל `mood` מתוך שמונה הבעות. הוסיפו הבעה = ענף חדש ב־
  [Roni.tsx](src/concepts/story/components/Roni.tsx) ומיפוי ב־`moodLabel`.

## מדיה ואנימציות

**כל מדיה בפרויקט חייבת להיות ברישיון פתוח לשימוש חופשי, כולל מסחרי. אין להוסיף
תמונה, סרטון או אנימציה בלי לוודא רישיון ולתעד אותו.**

מה יש כרגע:

| נכס | מקור | רישיון |
| --- | --- | --- |
| אנימציות האייקונים (`public/anim/*.json`) | [Noto Animated Emoji](https://googlefonts.github.io/noto-emoji-animation/) של Google | CC BY 4.0 — **מחייב ייחוס** |
| דמות רוני (`Roni.tsx`) | נכתבה לפרויקט | חלק מהריפו |
| שאר האיורים | אימוג'י מהמערכת + SVG/CSS | — |

CC BY 4.0 מחייב ייחוס, ולכן הקרדיט מופיע בשלושה מקומות שאסור להסיר:
[public/anim/NOTICE.txt](public/anim/NOTICE.txt), [README.md](README.md), ותחתית
[מסך התפריט](src/menu/MenuPage.tsx).

### איך זה עובד

- `npm run anim` ([scripts/fetch-animations.mjs](scripts/fetch-animations.mjs))
  סורק את האימוג'י שבשימוש ב־`src/data` וב־`src/concepts/story`, מוריד את מה שיש
  ל־Noto, וכותב מניפסט אל `src/data/animations.ts`. הריצו אותו אחרי שמוסיפים
  אימוג'י לתוכן. אל תערכו את המניפסט ביד.
- הכיסוי חלקי (כ־38 מתוך ~86 אימוג'י). זה בסדר:
  [AnimatedIcon](src/concepts/story/components/AnimatedIcon.tsx) נופל חזרה
  לאימוג'י הסטטי כשאין אנימציה, כשהקובץ נכשל, וכש־`prefers-reduced-motion` פעיל.
  **שום דבר בממשק לא תלוי באנימציה.**
- הקבצים נטענים lazy מ־`public/anim/`, ונגן ה־Lottie מיובא דינמית — הוא יורד רק
  במסכים שבאמת מציגים אנימציה (chunk נפרד של ~48KB gzip).
- יש תקרת גודל בסקריפט (150KB לקובץ) כדי לא לגרור אייקון של 320KB לטלפון של הורה
  במלר״ד.

### קונספט 1 נשאר סטטי — בכוונה

`calm` הוא קונספט של עומס חושי נמוך. אנימציות שם סותרות את כל הרעיון שלו, וההבדל
בין הקונספטים הוא בדיוק מה שהצוות אמור להשוות. אל "תשפרו" את `calm` עם אנימציות.

## עקרונות תוכן (מהאפיון והפידבק)

- **סטטוס + משמעות + אפשרות פעולה**, לא רק סטטוס. כל שלב אומר מה קורה, מה עלול
  להיות קשה, ומה אפשר לבקש.
- זמנים תמיד **בטווחים**, ותמיד עם הסתייגות שהסדר והזמנים משתנים לפי החלטת הצוות
  והעומס — כדי לא להקשות על הצוות.
- **שחרור הוא מעבר**, לא סוף. מסך נפרד, כולל מה קורה בבית ומעקב ברפואת קהילה.
- לכל סעיף בכרטיס ההתאמות יש **"אחר"** בטקסט חופשי (עד 150 תווים).
- המפה חייבת אינדקס חושי **רב־ערוצי** (רעש/אור/ריח/אנשים), לא רק "שקט/רועש".
- ניסוח לא מבטיח: "בסוף הבדיקה הם ידעו מה כדאי לעשות", לא "הם יעזרו לי להרגיש טוב".

## הרצה, בנייה ופריסה

```bash
npm install
npm run dev      # http://localhost:5173/
npm run build    # tsc -b && vite build → dist/
npm run preview
```

- פריסה אוטומטית ל־GitHub Pages בכל דחיפה ל־`main`
  ([.github/workflows/deploy.yml](.github/workflows/deploy.yml)).
- דומיין מותאם `accessible-er.mashlom.me`, ולכן `base: '/'` ב־
  [vite.config.ts](vite.config.ts). `public/CNAME` מועתק ל־`dist/`.
- **תמיד הריצו `npm run build` לפני commit** — ה־build כולל בדיקת טיפוסים.

## דפים לצוות בלבד

`#/calm/settings` ו־`#/story/settings` — דף ה־QR להדפסה כשילוט. בכוונה לא מקושרים
מהממשק להורים. `#/settings` הישן מפנה מחדש לקונספט הראשון.
